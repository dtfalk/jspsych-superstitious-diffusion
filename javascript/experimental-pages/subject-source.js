const redirect_trial = {
  type: jsPsychSurveyMultiChoice,
  questions: [
    {
      prompt: "Where were you redirected from?",
      options: ["Prolific", "Email", "Other"],
      required: true
    }
  ]
}
